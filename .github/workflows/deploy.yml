# GitHub Actions disabled — site deploys via Netlify only
# Do not re-enable this file — Netlify watches the main branch directly

name: Disabled

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploys handled by Netlify"
